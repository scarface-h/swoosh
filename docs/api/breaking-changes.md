# Breaking changes

No breaking changes have been published since contract version 1.0.0.

Potential future changes use a new `/api/v2` prefix or are recorded here before release. In v1, delivery identifiers are `DHAKA_INSIDE` and `OUTSIDE_DHAKA`; money fields in authoritative responses are decimal strings.
